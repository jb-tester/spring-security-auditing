## Spring Security and Events test:

1. Security: antMatchers(), mvcMatchers(), requestMatchers(), regexMatchers:
URL paths resolving, roles and authorities resolving
2. Events: events publishing in libraries
